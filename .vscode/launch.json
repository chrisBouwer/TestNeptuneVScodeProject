{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start project",
            "request": "launch",
            "program": "${workspaceFolder}/node_modules/.bin/webpack-cli",
            "windows": {
                "preLaunchTask": "Run on windows"
            },
            "args": ["serve", "-c", "webpack.dev.js"],
            "console": "integratedTerminal",
            "preLaunchTask": "Npm install",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "node"
        }
    ],
    "compounds": []
}
